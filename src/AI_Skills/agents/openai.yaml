interface:
  display_name: "HSAS Study Assistant"
  short_description: "Grounded course advice and deterministic planning"
  default_prompt: "Use $hsas-study-assistant to inspect my synchronized courses and explain what I should study next."
policy:
  allow_implicit_invocation: true
