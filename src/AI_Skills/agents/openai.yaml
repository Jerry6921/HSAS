interface:
  display_name: "HIQS Course Information"
  short_description: "Query local course facts and materials with citations"
  default_prompt: "Use $hiqs-course-information to answer my course question from the local HIQS database and downloaded materials. Cite the relevant course record, file, page, or slide; keep missing or conflicting information visible."
policy:
  allow_implicit_invocation: true
